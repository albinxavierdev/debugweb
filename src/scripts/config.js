// Configuration settings
const config = {
  isDevelopment: true,
  apiTimeoutMs: 10000,
  n8nWebhookUrl: 'https://example.com/webhook' // Replace with your actual webhook URL
};

// Export configuration
export default config; 